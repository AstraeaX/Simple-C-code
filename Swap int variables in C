#include <stdio.h>

void SwapValues(int* userVal1, int* userVal2) { 
   int n = *userVal1;
   int b = *userVal2;
   *userVal1 = b;
   *userVal2 = n;  
}

int main(void) {

   int x, y;
   scanf("%d %d", &x, &y);
   
   SwapValues(&x, &y);
   printf("%d %d\n", x, y);

   return 0;
}
