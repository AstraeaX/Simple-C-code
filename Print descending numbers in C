#include <stdio.h>

int main(void) {
   int numInput;
   int i;
   int j;

   scanf("%d", &numInput);

   for (int i = 0; i <= numInput; i++) {
      if (i != 0) {
         for (int j = 0; j < i; j++) {
            printf(" ");
         }
      }
      printf("%d\n", i);
   }

   return 0;
}
