#include <stdio.h>

int main(void) {
   int userNum;
   int i;
   int j;

   scanf("%d", &userNum);

   for (int i = 0; i <= userNum; i++) {
      if (i != 0) {
         for (int j = 0; j < i; j++) {
            printf(" ");
         }
      }
      printf("%d\n", i);
   }

   return 0;
}
